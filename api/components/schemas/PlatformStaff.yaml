type: object
properties:
  account:
    $ref: ./AccountSummary.yaml
  role:
    $ref: ./PlatformStaffRole.yaml
  grantedBy:
    $ref: ./UUID.yaml
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
required:
  - account
  - role
  - createdAt
  - updatedAt
