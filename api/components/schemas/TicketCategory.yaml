type: object
properties:
  id:
    $ref: ./UUID.yaml
  name:
    type: string
  zone:
    type: string
  price:
    type: integer
    format: int64
  quantity:
    type: integer
    format: int64
  priority:
    type: integer
    format: int64
  metadata:
    type: object
    additionalProperties: true
  maxBuyPerCustomer:
    type: integer
    format: int64
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
required:
  - id
  - name
  - zone
  - price
  - quantity
  - priority
  - metadata
  - maxBuyPerCustomer
  - createdAt
  - updatedAt
