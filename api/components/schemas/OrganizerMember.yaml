type: object
properties:
  account:
    $ref: ./AccountSummary.yaml
  role:
    $ref: ./OrganizerMemberRole.yaml
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
required:
  - account
  - role
  - createdAt
  - updatedAt
